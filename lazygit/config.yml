os:
  editCommand: "nvr -cc split -c on --remote-wait +'set bufhidden=wipe'"
gui:
  theme:
    selectedLineBgColor:
      - underline
    selectedRangeBgColor:
      - underline
git:
  paging:
    colorArg: always
    pager: delta --dark --paging=never --line-numbers
